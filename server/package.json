{
  "name": "restock-radar-server",
  "version": "1.0.0",
  "private": true,
  "description": "Restock Radar backend — sales velocity + depletion alerts from seed POS-style data",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "seed": "node generate-seed-data.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
