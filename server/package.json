{
  "name": "restock-radar-server",
  "version": "1.0.0",
  "private": true,
  "description": "Restock Radar backend — SQLite-backed retail stock tracker with restock-threshold alerts",
  "main": "server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "xlsx": "^0.18.5"
  }
}
